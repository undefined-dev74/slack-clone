import React from 'react';
import styled from 'styled-components';

const AppBody = () => {
  return <AppBodyContainer></AppBodyContainer>;
};

export default AppBody;

const AppBodyContainer = styled.div``;
